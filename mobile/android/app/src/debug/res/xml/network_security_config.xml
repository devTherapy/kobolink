<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only. Since Android 9 (API 28) cleartext (plain HTTP) traffic is
  blocked by default, and targetSdk 37 here enforces that. The app's default
  dev API_BASE_URL (see app/build.gradle.kts / local.properties.example) is
  http://10.0.2.2:3001/ — the standard Android-emulator alias for the host
  machine's `npm run dev` API — which is plain HTTP, so without this config
  every request fails immediately with "CLEARTEXT communication to 10.0.2.2
  not permitted".

  Scoped narrowly to the emulator alias plus localhost/127.0.0.1 (for a
  physical device reached via `adb reverse`). This file lives under
  src/debug and is wired in only by src/debug/AndroidManifest.xml, so it
  never ships in a release build — a blanket
  android:usesCleartextTraffic="true" in the main manifest would allow
  cleartext to ANY host and is exactly what this narrow scoping avoids.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
