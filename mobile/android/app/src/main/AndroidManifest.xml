<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The API client (LinksApi.resolvePublicLink, etc.) needs the network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".KobolinkApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Kobolink">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Kobolink">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          App Links (autoVerify VIEW/BROWSABLE/DEFAULT intent filter for
          https://pay.folusayo.com/l/*, uppercase SHA-256 asset link
          fingerprints, debug + Play App Signing certs) is M1's job, not M0's.
          M0 is scaffold + codegen + API client only — see PLAN.md.
        -->

    </application>

</manifest>
